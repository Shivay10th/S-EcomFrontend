/** @format */

import React from 'react';
import Base from '../core/Base';

const userProfile = () => {
	return (
		<Base title="UserProfilePage">
			<h1>This is user Profile</h1>
		</Base>
	);
};

export default userProfile;
